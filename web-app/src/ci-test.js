// Simple test file to trigger GitHub Actions
// This confirms the workflow is properly configured and tests pass
const testMessage = 'GitHub Actions CI/CD configured and working!';
console.log(testMessage);
